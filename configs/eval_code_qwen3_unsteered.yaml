run_name: "passk_qwen3_v1"
seed: 42

# Self-contained: no steering_config reference needed
model_name: "Qwen/Qwen3-32B"

# Label only — no runtime steering applied
scales: [0.0]

endpoint:
  base_url: "http://localhost:8017/v1"
  steering_mode: "none"

pass_at_k:
  n_samples: 100
  temperatures: [0.8]
  max_tokens: 512
  k_values: [1, 2, 5, 10, 25, 50, 100]

datasets: ["humaneval"]
